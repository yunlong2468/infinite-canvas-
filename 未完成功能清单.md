# 未完成功能清单

## 会话状态快照
- 时间: 2026-05-25
- 最后提交: `c096673` (撤回二次确认), `aa3b03e` (爬虫集成), `807eeee` (chat标签修复)
- 当前未提交修改: 精简系统提示 + load_skill 工具 + 默认SKILL + executeToolAsync模糊匹配

## 阶段一（刚完成，待测试验证）
- [x] `load_skill` 工具定义 + 执行逻辑
- [x] 精简 ORCHESTRATOR_SYSTEM
- [x] 默认 SKILL（角色设计指南 + 大纲设计指南）
- [x] executeToolAsync 模糊匹配
- [x] 前端 SSE 处理 load_skill 事件

## 阶段二（未开始）：分支 + 测试 + 合并
- [ ] `skill_optimizer` 生成 SKILL 后的完整流程：
  - 调配师询问用户是否测试
  - 用户确认后创建项目分支（完整快照：卷/章/角色/对话/版本）
  - 用户在新分支中测试 SKILL
  - 测试完成后右上角合并按钮
- [ ] 分支管理 API（创建/切换/合并/删除分支）
- [ ] 分支可视化 UI

## 阶段三（未开始）：冲突解决
- [ ] 冲突解决 UI：
  - 逐项选择保留版本A/版本B/两者都保留
  - 两者都保留时同名字段加编号后缀
  - 手动修复模式 + AI 建议
- [ ] 合并冲突检测逻辑

## 其他已知问题
- 刷新后子智能体流式气泡位置（frozen bubble 在上方 → 已修复 finalizeStreamingMsg）
- 系统消息 "{agent:orchestrator}调用{agent:xxx}" 渲染（_resolveToolAgent 已修复）
- 子智能体调用后 finalizeStreamingMsg 替换位置 → 改为 appendMsgToDOM
